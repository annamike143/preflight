[project]
name = "miro-fish-engine"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pypdf>=5,<6",
    "reportlab>=4,<5",
]

[tool.ruff]
target-version = "py314"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
