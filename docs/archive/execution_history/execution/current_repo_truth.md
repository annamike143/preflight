# Current Repository Truth

## Inspection Status
- Most recent completed packet inspection is COMPLETE for `P11-WS11-T06`.
- No Phase 11 packet inspection is currently in progress; Phase 11 is CLOSED after truthful post-close T06 refresh with a governed `release-ready` verdict.
- `P11-WS11-T06` is COMPLETE.
- `P11-WS11-T05` is COMPLETE.
- `P11-WS11-T04` is COMPLETE.
- `P11-WS11-T03` is COMPLETE.
- `P11-WS11-T02` is COMPLETE.
- `P11-WS11-T01` is COMPLETE.
- `P10-WS11-T01` is COMPLETE.
- `P10-WS10-T04` is COMPLETE.
- `P10-WS10-T03` is COMPLETE.
- `P10-WS10-T02` is COMPLETE.
- `P10-WS10-T01` is COMPLETE.
- `P09-WS11-T01` is COMPLETE.
- `P09-WS09-T04` is COMPLETE.
- `P09-WS09-T02`, `P09-WS09-T01`, `P08-WS11-T01`, `P08-WS08-T04`, `P08-WS08-T03`, `P08-WS08-T02`, `P08-WS08-T01`, `P07-WS11-T02`, and `P07-WS11-T03` are COMPLETE; `P07-WS11-T01`, `P07-WS07-T04`, `P07-WS07-T05`, and `P07-WS07-T06` remain COMPLETE.
- Phase 8 is now CLOSED after the completed Wave 4 and exit-gate packets, Phase 9 is now CLOSED after the completed Wave 4 and exit-gate packet, and Phase 10 is now CLOSED after the completed Wave 4 and exit-gate packets; Phase 11 is now CLOSED after the completed T06 release-refresh packet.
- This file must distinguish KNOWN from UNKNOWN.
- Unknown must not be silently treated as decided.

## Current Known Truth
- Live governing product document exists:
  - `Document_A/document_a_master_project_creative_brief_v2a_final_draft_2.md`
- Live governing execution document exists:
  - `Document_B/document_b_master_execution_plan_b1_0_final_draft_2.md`
- Live technical-annex document exists:
  - `Document_C/document_c_engineering_annex_pack_c1_0_final_draft_1.md`

## Current Known Execution Truth
- The current execution focus is truthful Phase 11 closure accounting after `P11-WS11-T06` consumed the governed Windows-only scope revision and refreshed the final release record to `release-ready`.
- The authoritative execution operating-system guide is `execution/README.md`.
- Root `README.md` is absent and is not used for execution control.
- The execution operating system now uses a lean live model centered on `execution/progress.md` and `execution/live_execution_log.md`, while preserving explicit packet lifecycle states, zero-warning gate discipline, and a mandatory critique pass.
- Packet history is now indexed in `execution/packet_registry.md`.
- Most recently completed packet is `execution/task_packets/p11_ws11_t06_execute_phase_11_windows_only_scope_realignment_release_record_refresh_and_final_verdict_revalidation.md`.
- No Phase 11 packet is currently active.
- `execution/phase_11_master_execution_plan.md` now exists and governs Phase 11 wave order, matrix instantiation, acceptance/regression execution, priority red-team review, blocker and residual-issue accounting, and the final governed release verdict.
- `execution/phase_11_validation/acceptance_mapping.md`, `execution/phase_11_validation/qa_regression_matrix.md`, `execution/phase_11_validation/red_team_matrix.md`, `execution/phase_11_validation/blocker_and_residual_issue_log.md`, and `execution/phase_11_validation/release_readiness_assessment.md` now exist as the repo-local Phase 11 validation artifact pack.
- `execution/phase_11_validation/acceptance_mapping.md`, `execution/phase_11_validation/qa_regression_matrix.md`, and `execution/phase_11_validation/red_team_matrix.md` now contain the final W03/W04/W05/T06 evidence story; `execution/phase_11_validation/blocker_and_residual_issue_log.md` and `execution/phase_11_validation/release_readiness_assessment.md` now record the historical `BL-001` resolution and the current `release-ready` verdict.
- `execution/task_packets/p11_ws11_t06_execute_phase_11_windows_only_scope_realignment_release_record_refresh_and_final_verdict_revalidation.md` is COMPLETE.
- `execution/task_packets/p11_ws11_t05_execute_phase_11_release_readiness_gate_verdict_and_closure.md` is COMPLETE.
- `execution/task_packets/p11_ws11_t04_execute_phase_11_priority_red_team_review_blocker_triage_and_residual_issue_accounting.md` is COMPLETE.
- `execution/task_packets/p11_ws11_t03_populate_phase_11_acceptance_mapping_execute_regression_reruns_and_collect_evidence.md` is COMPLETE.
- `execution/task_packets/p11_ws11_t02_establish_phase_11_acceptance_mapping_qa_matrix_red_team_matrix_regression_inventory_and_release_aggregation_foundation.md` is COMPLETE.
- `execution/task_packets/p11_ws11_t01_establish_phase_11_qa_matrix_red_team_review_and_release_readiness_entry_basis_and_master_execution_blueprint.md` is COMPLETE.
- `execution/task_packets/p10_ws10_t04_implement_verified_runtime_delivery_update_apply_gating_and_artifact_rejection_hardening.md` is COMPLETE.
- `P10-WS10-T04` now spans more than the original shell-first slice: it still materializes ProjectDirs-backed runtime installation and rejected-candidate persistence, manifest-driven hydration from `MIRO_FISH_RUNTIME_DELIVERY_MANIFEST`, expected-file verification, compatibility rejection, rollback-safe metadata commit, trust-manifest validation against the installed package, hydration-required lifecycle or readiness truth grounded in actual runtime presence and integrity, and idle-only updater apply-gate truth.
- `shell/src/native_app_updater.rs`, `shell/src/updater_contract.rs`, `shell/src/local_interface_contract.rs`, and `shell/src/main.rs` now also materialize an explicit shell-owned updater preparation path so `prepare_native_app_updater` can perform real signed download-plus-verification progression into truthful `UpdateReady`, `UpdateRejected`, or `UpdaterError` outcomes without collapsing discovery into ready-state truth.
- `shell/src/native_app_updater.rs`, `shell/src/updater_contract.rs`, `shell/src/local_interface_contract.rs`, and `shell/src/main.rs` now also materialize the final explicit shell-owned updater apply path so `apply_native_app_updater` can install only a staged verified `UpdateReady` artifact, transition truthfully through `ApplyingUpdate`, `UpdateApplied`, or `UpdaterError`, and request app restart after successful install without mutating active runs.
- `app/src/App.tsx` and `app/src/App.wave10.test.tsx` now render bounded Phase 10 diagnostics actions for refreshing updater discovery, preparing a verified update candidate, and applying a verified update candidate, and they surface ready, rejected, error, release-note, publication-date, apply-gate, and restart-request truth while remaining presentation-only.
- `commercial_backend/src/commercial_backend/cli.py`, `commercial_backend/src/commercial_backend/models.py`, `commercial_backend/src/commercial_backend/service.py`, `commercial_backend/tests/test_cli.py`, and `commercial_backend/tests/test_runtime_delivery.py` now materialize a bounded `resolve-runtime-delivery-manifest` support surface that validates license usability and returns an authorized manifest reference plus fingerprint; `shell/src/runtime_hydration.rs` can fall back to that repo-local backend surface when a direct runtime manifest path is not configured.
- `execution/task_packets/p10_ws10_t03_implement_native_app_updater_discovery_availability_truth_and_safe_coordination.md` is COMPLETE.
- `execution/task_packets/p10_ws10_t02_establish_updater_lifecycle_runtime_delivery_contract_and_trust_surface_foundation.md` is COMPLETE.
- `execution/task_packets/p10_ws10_t01_establish_phase_10_updater_runtime_delivery_and_distribution_hardening_entry_basis_and_master_execution_blueprint.md` is COMPLETE.
- `execution/task_packets/p09_ws11_t01_execute_phase_9_validation_critique_and_exit_gate.md` is COMPLETE.
- `execution/task_packets/p09_ws09_t04_implement_app_close_shutdown_integration_abnormal_termination_residual_detection_and_startup_janitor_execution.md` is COMPLETE.
- `execution/task_packets/p09_ws09_t02_establish_cleanup_outcome_scope_mapping_and_janitor_truth_surface_foundation.md` is COMPLETE.
- `execution/task_packets/p08_ws08_t04_implement_pdf_generation_report_availability_and_local_access_integration.md` is COMPLETE.
- `execution/task_packets/p09_ws09_t01_establish_phase_9_cleanup_shutdown_and_startup_janitor_hardening_entry_basis_and_master_execution_blueprint.md` is COMPLETE.
- `P10-WS11-T01` is COMPLETE.
- Phase 3 is closed honestly.
- Phase 4 is closed honestly.
- Phase 5 is closed honestly.
- Phase 6 is closed honestly.
- Phase 7 is closed honestly.
- Phase 8 is closed honestly.
- Phase 9 is closed honestly.
- Phase 10 is closed honestly after `P10-WS11-T01` proved the governed acceptance story against the live updater and runtime-delivery implementation.
- Phase 11 is closed honestly after `P11-WS11-T06` resolved the historical `BL-001` blocker via the governed Windows-only scope revision and selected the governed `release-ready` verdict; `P11-WS11-T05` remains the truthful blocked record under the earlier contract.
- `execution/phase_08_master_execution_plan.md` now exists and governs Phase 8 wave order, finalization/report ownership split, required report contents basis, PDF/output-boundary rules, report availability/access truth, and later validation sequencing.
- `execution/phase_09_master_execution_plan.md` now exists and governs Phase 9 wave order, cleanup/shutdown/janitor ownership split, cleanup-scope mapping basis, report-preservation exclusions, and later validation sequencing.
- `execution/phase_10_master_execution_plan.md` now exists and governs Phase 10 wave order, updater/runtime-delivery ownership split, verification-before-apply or use rules, active-run non-mutation constraints, and later validation sequencing.
- `execution/phase_11_master_execution_plan.md` now exists and governs Phase 11 wave order, matrix instantiation, acceptance/regression execution, priority red-team review, blocker and residual-issue accounting, and final verdict sequencing.
- `execution/phase_07_master_execution_plan.md` now exists and governs Phase 7 wave order, canonical run lifecycle exposure, ordered event retrieval semantics, live monitoring UI boundaries, the shell-owned in-flight snapshot prerequisite, and later degradation/cancellation packetization.
- `execution/phase_05_master_execution_plan.md` now exists and governs Phase 5 wave order, bounded seed-intake handling, Preflight outcome semantics, requested-versus-effective plan separation, stale invalidation, and later packetization.
- `execution/phase_06_master_execution_plan.md` now exists and governs Phase 6 wave order, fresh run and workspace isolation, Preflight-lineage start basis, runtime persona generation, exactly one Moderator, 3-to-5-speaker normal rounds, bounded memory, transcript-archive distinction, and token or duration tracking packetization.
- `execution/phase_04_master_execution_plan.md` now exists and governs Phase 4 wave order, bounded provider-secret handling, provider-readiness separation, and later packetization.
- `P05-WS05-T01` completed the Phase 5 entry freeze by authoring the bounded seed-intake and Preflight path plus the Phase 5 master execution blueprint without implementing seed-intake or Preflight code yet.
- `P05-WS05-T02` completed the bounded contract and run-eligibility-foundation work by materializing shell-owned seed-intake and Preflight contract surfaces, invalidation semantics, lifecycle/startup/interface/trust-boundary integrations, and run-start-eligibility truth without implementing real Preflight behavior yet.
- `P05-WS05-T03` completed the bounded Wave 3 implementation work by materializing real single-seed intake, file metadata capture, PDF/DOCX/TXT/Markdown-only enforcement, 10 MiB file-size enforcement, 1,000-character minimum extracted-text enforcement, English-only rejection at the 0.80 confidence threshold, and deterministic known document-validation rejection behavior without implementing provider/model feasibility, effective-plan derivation, downgrade decisioning, or run start.
- `P05-WS05-T04` completed the bounded Wave 4 implementation work by materializing shell-owned provider/model planning-context evaluation, effective-plan derivation, explicit approved versus approved-with-downgrade versus rejected outcomes, downgrade-acceptance recording, stale invalidation, truthful run-start gating, and real frontend Preflight assertion coverage.
- `P05-WS11-T01` completed the bounded Wave 5 exit-gate work by rerunning the full Phase 5 shell and frontend gate set cleanly, proving `AC-031` through `AC-040` against the live seed-intake and Preflight implementation, and recording an honest Phase 5 closure decision without adding new product-code scope.
- `P06-WS06-T01` completed the bounded Phase 6 entry work by authoring the Phase 6 master execution blueprint, freezing fresh-run and workspace-isolation semantics, freezing Preflight-lineage start semantics, freezing runtime persona and Moderator semantics, freezing bounded-memory and transcript-archive separation, and leaving Phase 6 between packets without claiming implementation code.
- `P06-WS06-T02` completed the bounded Phase 6 Wave 2 implementation work by materializing run/session contract surfaces, workspace-isolation boundary surfaces, Preflight-lineage start surfaces, run-start-foundation truth, lifecycle and trust-boundary integrations, startup and supervision integrations, local-interface and command-note integrations, and a bounded frontend truth update without implementing real run/session creation, workspace allocation, seed execution ingestion, or active moderated execution.
- `P06-WS06-T03` completed the bounded Phase 6 Wave 3 implementation work by materializing real fresh run/session creation, fresh isolated workspace allocation, current Preflight-lineage capture, execution-seed staging, run-session state truth, and bounded frontend run-start integration without implementing runtime persona generation, Moderator logic, moderated rounds, bounded-memory execution, token or duration tracking, or live monitoring.
- `P06-WS06-T04` completed the bounded Phase 6 Wave 4 implementation work by materializing the new `engine/` package, runtime-generated personas from approved staged seed context, exactly one Moderator basis, stable persona labels, bounded memory foundation, transcript-archive separation, shell-owned engine bootstrap wiring, and truthful frontend status rendering without implementing moderated rounds, token or duration tracking, live monitoring, final-report behavior, or cleanup hardening.
- `P06-WS06-T05` completed the bounded Phase 6 Wave 5 implementation work by materializing the one-shot Moderator-controlled moderated execution path, per-round 3-to-5-speaker selection, speaker-attributed transcript turns, bounded continuity mutation during execution, run-local token/duration tracking, truthful shell supervision state, and finished run-session snapshots without implementing Phase 7 live monitoring fidelity, Phase 8 final-report behavior, or Phase 9 cleanup hardening.
- `P06-WS11-T01` completed the bounded Phase 6 Wave 6 exit-gate work by rerunning the full shell, frontend, and engine gate families cleanly, proving `AC-041` through `AC-059` against the live Phase 6 implementation, completing the critique pass without in-scope remediation, and closing Phase 6 honestly.
- `P07-WS07-T01` completed the bounded Phase 7 entry work by authoring `execution/phase_07_master_execution_plan.md`, freezing the canonical state-exposure basis, ordered event-envelope and retrieval semantics, live monitoring UI boundary, and degradation/cancellation packetization rules, and returning Phase 7 to a truthful between-packets posture without implementation code.
- `P07-WS07-T02` completed the bounded Phase 7 Wave 2 implementation work by materializing `shell/src/run_lifecycle_state.rs`, `shell/src/run_status_contract.rs`, and `shell/src/run_event_contract.rs`, extending the shell-owned lifecycle, trust-boundary, startup, interface, supervision, and run-session contract surfaces with canonical lifecycle and monitoring-contract truth, updating the frontend to render that truth, and keeping real ordered event history, live monitoring synchronization, paused/degraded behavior, and controlled cancellation execution explicitly out of scope.
- `P07-WS07-T03` completed the bounded Phase 7 Wave 3 implementation work by materializing `shell/src/run_status_projection.rs` and `shell/src/run_event_history.rs`, integrating shell-owned status projection and ordered event retrieval through the run-status, run-event, run-session, lifecycle, supervision, interface, startup, and command surfaces, enriching engine transcript and token/duration artifacts with truthful local `occurred_at` evidence, rerunning the applicable shell and engine gate families cleanly, and returning Phase 7 to a truthful between-packets posture without implementing Wave 4 live-monitoring UI rendering or Wave 5 degradation/cancellation behavior.
- `P07-WS07-T04` is now COMPLETE because the previously delivered frontend live-monitoring UI surface, current phase visibility, incremental chronology feed rendering, bounded polling, and Wave 7 frontend regression coverage now sit on top of the completed T05 shell prerequisite.
- `P07-WS07-T05` is now COMPLETE after materializing the bounded shell-owned in-flight run/session snapshot publication path before later degradation/cancellation work proceeds.
- `P07-WS07-T06` completed the bounded Phase 7 Wave 6 implementation work by materializing shell-owned paused rate-limit/network truth, bounded retry visibility and retryable-versus-terminal distinction where known, implemented cancel and controlled-shutdown request paths, supplemental interruption chronology, and updated frontend monitoring controls over the completed T04/T05 basis.
- `P07-WS11-T01` completed the bounded Phase 7 exit-gate work by rerunning the full shell, frontend, and engine gate families cleanly, mapping `AC-060` through `AC-085`, and identifying runtime budget-hit/duration-cap control plus truthful capped/finalizing projection as the remaining closure blocker.
- `P07-WS11-T02` completed the bounded recovery packet by materializing runtime token-budget and duration-cap enforcement, the run-local `execution_phase.json` artifact, shell-owned constrained completion and finalization projection, updated event/status/UI truth, and clean shell/frontend/engine validation evidence.
- `P07-WS11-T03` completed the bounded Phase 7 revalidation packet by remapping `AC-060` through `AC-085` against the recovered repo truth, fixing a final shell Clippy compliance issue in `shell/src/run_status_projection.rs`, rerunning the full shell gate set cleanly, and closing Phase 7 honestly.
- `P08-WS08-T01` completed the bounded Phase 8 entry packet by authoring `execution/phase_08_master_execution_plan.md`, freezing the finalization/report-generation basis, and activating Phase 8 between packets without adding report feature code.
- `P08-WS08-T02` completed the bounded Phase 8 Wave 2 packet by materializing `shell/src/run_finalization_contract.rs`, `shell/src/report_output_contract.rs`, `shell/src/report_storage_boundary.rs`, and `shell/src/report_access_contract.rs`, integrating those shell-owned surfaces through lifecycle, startup, trust-boundary, local-interface, app-lifecycle, and Tauri command exposure, updating the frontend to render the new Phase 8 contract-foundation section, and adding Wave 8 regression coverage without implementing real report synthesis, PDF generation, or local artifact access behavior.
- `shell/src/run_finalization_contract.rs`, `shell/src/report_output_contract.rs`, `shell/src/report_storage_boundary.rs`, and `shell/src/report_access_contract.rs` now define the bounded Phase 8 contract vocabulary for finalization markers, required report sections, output classes, durable-versus-transient report boundary meaning, and report availability/access failure classifications.
- `app/src/App.tsx`, `app/src/App.test.tsx`, `app/src/App.wave7.test.tsx`, and `app/src/App.wave8.test.tsx` now render and validate the shell-owned Phase 8 contract foundation without implying report availability, PDF existence, or local file accessibility.
- `P08-WS08-T03` completed by materializing `engine/src/miro_fish_engine/report_data_assembly.py`, extending engine finalization so successful and constrained-completion runs assemble required report data from actual bounded-memory, transcript, and tracked-usage artifacts, and wiring shell-owned report-generation success versus failure classification without implementing final PDF generation or local report access.
- `P08-WS08-T04` completed by materializing the final PDF generation path, durable local report artifact output, shell-owned report availability and local access behavior, and bounded frontend report-access rendering.
- `P08-WS11-T01` completed by rerunning the full Phase 8 gate family cleanly after one in-scope frontend lint-baseline remediation, executing a real artifact-layer report-generation smoke for healthy and constrained-completion paths, mapping `AC-086` through `AC-091` plus `AC-093` and `AC-097`, and recording an honest Phase 8 closure decision plus Phase 9 entry criteria.
- `P09-WS09-T01` completed by authoring `execution/phase_09_master_execution_plan.md`, freezing the bounded cleanup/shutdown/janitor path, report-preservation exclusions, and no-resume constraints, and returning Phase 9 to an honest between-packets posture without product-code changes.
- `P09-WS09-T02` completed by materializing `shell/src/run_cleanup_contract.rs`, `shell/src/cleanup_scope_mapping.rs`, and `shell/src/startup_janitor_contract.rs`, integrating the new cleanup/janitor truth across lifecycle, startup, trust-boundary, local-interface, status, event, shutdown, and report-boundary surfaces, and updating the Reports screen to render the new shell-owned Phase 9 contract foundation.
- `P09-WS09-T03` completed by materializing `shell/src/run_cleanup_execution.rs`, real shell-owned terminal cleanup triggering for `Completed`, `CompletedBudgetCapped`, `Canceled`, and `FailedTerminal` paths, cleanup markers on `RunSessionStateSnapshot`, cleanup-aware lifecycle/status/event truth, preserved-report survival outside cleanup scope, and bounded frontend cleanup rendering.
- `P09-WS09-T04` completed by materializing `shell/src/abnormal_termination_residual.rs` and `shell/src/startup_janitor_execution.rs`, wiring shell-owned controlled-shutdown handoff to residual markers and next-launch janitor behavior, updating startup/lifecycle/trust/report surfaces to expose that truth, and preserving the ban on crash-resume or automatic OS-level close interception beyond the bounded controlled-shutdown request surface.
- The current shell one-shot run path in `shell/src/run_session_start.rs` now publishes truthful in-flight run/session snapshots at shell-known checkpoints for identity allocation, workspace allocation, execution-seed staging, foundation readiness, active execution handoff, and final completion without holding the stored snapshot mutex across the whole one-shot execution path.
- `run_session_state`, `run_status_state`, and `run_event_history` can now expose current run/session identity and truthful pending-history basis during active execution even when ordered chronology is not yet complete.
- `shell/src/run_session_start.rs` now returns `execution_started` immediately, supervises the active Python child under shell authority, publishes runtime-control flags and supplemental control events during retry, cancellation, and shutdown transitions, and uses a workspace-local `execution_control.json` file plus graceful-then-forceful termination fallback to stop hidden post-cancel progression.
- `shell/src/run_lifecycle_state.rs`, `shell/src/run_status_contract.rs`, `shell/src/run_status_projection.rs`, `shell/src/run_event_contract.rs`, and `shell/src/run_event_history.rs` now expose paused rate-limit/network truth, retryable-versus-terminal failure distinction where known, implemented cancellation truth, live control-event capture, merged interruption chronology, and real cleanup-aware lifecycle/status/event truth without collapsing cleanup outcome into the underlying run outcome.
- `shell/src/preflight_submission.rs` and `shell/src/preflight_effective_plan.rs` still require a nonzero max token budget and reject requested plans whose estimated duration exceeds the hard two-hour cap before execution begins, while the recovered Phase 7 runtime path now also enforces those caps during active execution.
- `engine/src/miro_fish_engine/moderated_execution.py`, `engine/src/miro_fish_engine/session_execution.py`, and `engine/src/miro_fish_engine/token_duration_tracking.py` now stop scheduling new normal rounds once a governed runtime cap is reached, preserve truthful constrained completion, and write `execution_phase.json` with finalization timestamps and cap reason.
- `shell/src/run_session_start.rs`, `shell/src/run_lifecycle_state.rs`, `shell/src/run_status_projection.rs`, and `shell/src/run_event_history.rs` now project `Finalizing` and `CompletedBudgetCapped` from runtime control evidence, including finalization-started/finalization-completed chronology and near-cap warning notes.
- `CompletedBudgetCapped` and `Finalizing` are now real shell-owned runtime projections across engine result shape, shell lifecycle/status/event history, and frontend monitoring; `Cleaning`, `Cleaned`, and `CleanupFailed` are now also real shell-owned cleanup projections across governed terminal cleanup paths.
- `P07-WS11-T01` also confirmed that live monitoring, chronology, paused/degraded truth, cancellation handling, and graceful-then-forceful shutdown coordination remain real across shell, engine, and frontend surfaces.
- `shell/src/controlled_shutdown.rs`, `shell/src/app_lifecycle.rs`, `shell/src/local_interface_contract.rs`, and `shell/src/engine_supervision.rs` now reflect materialized active-run shutdown coordination, implemented cancellation command truth, and shell-owned supervision flags rather than scaffold-only interruption posture.
- `engine/src/miro_fish_engine/moderated_execution.py` and `engine/src/miro_fish_engine/session_execution.py` now honor shell-written execution-control signals at safe round and turn boundaries and emit structured cancellation or shutdown markers for shell classification.
- `app/src/App.tsx` now renders shell-owned cancel and controlled-shutdown controls, retry or degradation banners, accepted-cancellation messaging, and updated Wave 7 monitoring truth without gaining lifecycle authority.
- Phase 8 now has real transient report-data assembly, viability-score generation, transcript appendix synthesis, governed PDF generation, durable local report artifact output, shell-owned report availability and local access integration, and bounded frontend report-access rendering in product code; Phase 9 Wave 3 terminal cleanup hardening now also exists.
- Phase 9 implementation now exists through Waves 2 through 4, and `P09-WS11-T01` completed the exit-gate pass cleanly: cleanup and janitor contract foundations are complete, real terminal cleanup triggering and cleanup-result truth now exist in product code, app-close cleanup coordination plus startup janitor execution and no-resume-preserving janitor hardening are complete, and the Phase 9 acceptance gate is now satisfied without overclaiming forensic deletion, hidden continuity, or release readiness.
- The Phase 6 entry critique pass completed cleanly across the touched execution-planning files and found no contradiction or in-scope inconsistency that required remediation before closeout.
- The Phase 6 Wave 2 critique pass completed cleanly across the touched shell, frontend, and execution-state files and found no material contradiction or in-scope defect that required remediation before closeout.
- The Phase 6 Wave 3 critique pass completed cleanly across the touched shell, frontend, and execution-state files and found no material contradiction or in-scope defect that required remediation before closeout.
- The Phase 6 Wave 4 critique pass completed cleanly across the touched shell, frontend, engine, and execution-state files and found no material contradiction or in-scope defect that required remediation before closeout.
- The Phase 6 Wave 5 critique pass completed cleanly across the touched shell, frontend, engine, and execution-state files and found no material contradiction or in-scope defect that required remediation before closeout.
- The Phase 7 entry critique pass completed cleanly across the touched execution-state and planning files and found no contradiction, hidden-scope expansion, or in-scope inconsistency that required remediation before closeout.
- The Phase 7 Wave 2 critique pass completed cleanly across the touched shell, frontend, and execution-state files and found no material contradiction or in-scope defect that required remediation before closeout.
- The Phase 7 Wave 3 critique pass completed cleanly across the touched shell, engine, test, and execution-state files and found no material contradiction or in-scope defect that required remediation before closeout.
- The Phase 8 Wave 2 critique pass completed cleanly across the touched shell, frontend, and execution-state files and found no material contradiction, truth collapse, or in-scope defect that required remediation before closeout.
- The completed Wave 4 packet now uses a jsdom-backed frontend test environment with real Vitest assertions around the shell-owned Preflight submission flow in `app/src/App.test.tsx` and `app/src/App.wave4.test.tsx`.
- The 2026-04-15 governance realignment froze the supported seed-document constants to PDF, DOCX, TXT, and Markdown; 10 MiB maximum file size; 1,000 normalized extracted-character minimum; and non-English rejection at English-confidence below 0.80.
- The 2026-04-15 governance realignment also froze the MVP runtime/update split: app-shell updates use the native app-updater path, while Python runtime delivery remains a separate shell-owned managed runtime-package hydration flow authorized by the commercial backend and verified before use.
- `shell/src/native_app_updater.rs`, `shell/src/updater_contract.rs`, and `shell/src/main.rs` now materialize the bounded Phase 10 Wave 3 native app-updater discovery path by wiring `tauri-plugin-updater`, resolving signed discovery config from `MIRO_FISH_APP_UPDATER_ENDPOINTS` plus `MIRO_FISH_APP_UPDATER_PUBKEY`, and keeping shell-owned `CheckingForUpdates`, `NoUpdateAvailable`, `UpdateAvailable`, and `UpdaterError` classification distinct from ready/apply behavior.
- `shell/src/runtime_package_contract.rs`, `shell/src/runtime_hydration.rs`, `shell/src/runtime_presence.rs`, `shell/src/runtime_integrity.rs`, `shell/src/runtime_delivery_contract.rs`, `shell/src/runtime_readiness.rs`, `shell/src/startup_checks.rs`, `shell/src/app_lifecycle.rs`, `shell/src/local_interface_contract.rs`, `shell/src/native_app_updater.rs`, `shell/src/updater_contract.rs`, and `shell/src/run_status_contract.rs`, plus `shell/src/main.rs`, now materialize the first shell-only Phase 10 Wave 4 slice by persisting runtime installation and rejected-candidate metadata under the app-controlled support location, hydrating runtime artifacts from the `MIRO_FISH_RUNTIME_DELIVERY_MANIFEST` allowlist, verifying expected files and compatibility before runtime use, restoring the previous runtime if metadata commit fails after file movement, validating trust metadata against the installed package, surfacing hydration-required lifecycle/readiness truth from actual presence and integrity, and exposing idle-only updater apply-gate truth without mutating active runs.
- `shell/src/runtime_readiness.rs`, `shell/src/app_lifecycle.rs`, `shell/src/startup_checks.rs`, and `shell/src/run_status_contract.rs` now keep hydration-required, hydrating, rejected-candidate, and mutation-blocked runtime truth distinct from updater discovery, run lifecycle, report, and cleanup meaning.
- `shell/src/native_app_updater.rs`, `shell/src/updater_contract.rs`, and `shell/src/run_status_contract.rs` now also include a focused shell-only proof path that injects a mocked completed discovery result into run-status projection, proving active-run updater coordination after discovery without mutating global updater state or collapsing run-lifecycle truth into updater truth.
- `app/src/App.tsx` and `app/src/App.wave10.test.tsx` now invoke the shell-owned updater discovery command and render bounded Phase 10 configuration readiness, no-update versus update-available versus error truth, and active-run-safe apply deferral without implying download, verification, apply, restart, or runtime-delivery execution.
- The current repo now has bounded Phase 10 native app-updater discovery, real shell-owned updater download and verification progression into `UpdateReady` or `UpdateRejected`, real shell-owned updater apply plus restart-request execution over verified `UpdateReady` state, validated bounded frontend diagnostics actions for that updater truth, and bounded commercial-backend runtime-delivery manifest authorization support in product code.
- The current repo now has executed Phase 10 exit-gate validation and closure evidence plus a closed Phase 11 record containing populated acceptance, QA/regression, red-team, blocker-resolution, and release artifacts; it now has release-ready evidence for the governed Windows-only MVP scope, and it still lacks durable release-operations automation and hosted signing automation as deferred infrastructure work rather than active Phase 11 blockers.
- The former open questions around local support storage, commercial backend implementation surface, runtime/update base strategy, canonical validation baseline, and seed-intake constants are now closed in the execution OS.
- `P04-WS04-T01` completed the Phase 4 entry freeze by authoring the bounded provider-secret path and master execution blueprint without implementing provider-configuration code yet.
- `P04-WS04-T02` completed the Phase 4 Wave 2 foundation work by materializing shell-owned provider configuration contract, provider-secret boundary, and provider-readiness truth surfaces without implementing real provider-key storage or retrieval behavior.
- `P04-WS04-T03` completed the Phase 4 Wave 3 implementation work by materializing the real shell-owned provider secure-store abstraction, bounded provider support-state persistence, provider configuration submission path, retrieval mediation, truthful provider-readiness state derivation, and the bounded frontend provider setup workflow.
- `P04-WS11-T01` completed the bounded validation, critique, and exit-gate work by rerunning the Phase 4 shell and frontend gate suite cleanly, proving `AC-025` through `AC-030`, and closing Phase 4 honestly.
- Phase 5 entry now freezes one-seed-document-only input handling, Preflight separation from readiness domains, requested-versus-effective plan semantics, and stale invalidation as the bounded basis for later implementation packets.
- The bounded local activation-artifact path is now frozen at the storage-domain level.
- Raw device-bound JWT material belongs in OS-native secure storage under shell authority.
- Bounded non-secret activation validation support data belongs in app-controlled local persistent support storage under shell authority.
- Provider secrets are now frozen to the same OS-native secure storage authority boundary under shell control.
- Any bounded non-secret provider-configuration support data is now frozen to the app-controlled local persistent support domain under shell authority if later implementation requires it.
- `shell/src/provider_configuration_contract.rs` now defines a materialized bounded provider-configuration contract surface for OpenAI and Google, including request-shape expectations, failure classifications, secure-store commit requirements, and truthful local configuration success without implying Preflight readiness.
- `shell/src/provider_secure_store.rs` now materializes the shell-owned OS-native provider secure-store abstraction with per-provider commit, retrieval, and delete behavior and without exposing secure-store handles to the frontend.
- `shell/src/provider_support_store.rs` now materializes bounded JSON-backed provider support-state persistence in the app-controlled local support domain without storing raw provider secrets.
- `shell/src/provider_configuration_submission.rs` now materializes the real shell-owned provider configuration submission path, rollback discipline, provider-state truth derivation, shell-only provider-secret retrieval mediation, deselected-provider secret clearing during provider switches, and shell-boundary unsupported-provider classification.
- `shell/src/provider_storage_boundary.rs` now reflects the real provider secure-store and provider support-state persistence behavior while preserving the forbidden ordinary persistence paths.
- `shell/src/provider_readiness.rs` now derives provider readiness from actual local provider support-state and secure-store truth while keeping provider readiness separate from runtime readiness, activation readiness, and Preflight eligibility.
- `shell/src/preflight_contract.rs` now defines the expanded bounded seed-intake and Preflight contract surface for one seed document, requested and effective plan snapshots, estimated usage, provider/model planning context, downgrade disclosure and acceptance, broader rejection families, stale invalidation semantics, and the forbidden durable raw-seed archival path.
- `shell/src/preflight_document_validation.rs` now materializes shell-owned seed-document kind detection, TXT or Markdown text extraction, DOCX extraction, PDF extraction, whitespace normalization, SHA-256 fingerprinting, size enforcement, minimum extractable-text enforcement, and English-confidence rejection classification.
- `shell/src/preflight_effective_plan.rs` now materializes bounded provider-default model mapping, effective-plan derivation, estimated token and duration heuristics, downgrade warning generation, and rejection when even the minimum governed plan cannot fit the requested budget.
- `shell/src/preflight_feasibility.rs` now materializes bounded provider/model planning-context evaluation, rejection and invalidation messaging, evaluation-basis capture, and stale-result comparison logic using provider support-state, secure-store truth, activation-readiness truth, runtime-readiness truth, and requested-plan inputs without claiming live provider/model reachability probing.
- `shell/src/preflight_submission.rs` now materializes the shell-owned single-seed submission boundary, one-pending-seed replacement semantics, requested-plan capture, approved versus approved-with-downgrade versus rejected versus technical-failure outcomes, persisted evaluation basis, and downgrade-acceptance recording.
- `shell/src/preflight_readiness.rs` now derives the canonical Preflight boundary state from the real shell-owned submission state, including selected-seed metadata, requested plan, effective plan, estimated usage, provider/model context, downgrade disclosure, invalidation reasons, and truthful current run-start eligibility.
- `shell/src/run_session_contract.rs` now defines the bounded shell-owned run/session start contract surface, including shell-owned identity generation requirements, current-eligible-Preflight dependency, coarse run-state classes, and the explicit distinction between start-blocked and technical-failure outcomes without claiming real run/session creation.
- `shell/src/run_workspace_boundary.rs` now defines the bounded per-run workspace-isolation and run-support-metadata boundary, including fresh-workspace requirements, no-active-reuse rules, transcript-archive separation, and explicit prohibition of hidden continuation or crash-resume behavior without claiming real allocation or cleanup.
- `shell/src/run_start_lineage.rs` now defines the bounded Preflight-derived run-start lineage surface, carrying approved seed fingerprint, requested-plan, effective-plan, and provider/model planning context readiness without embedding raw seed bytes, transcript replay payloads, or prior-run memory.
- `shell/src/run_start_readiness.rs` now defines the shell-owned run-start foundation truth surface separately from runtime readiness, activation readiness, provider readiness, Preflight eligibility, and active execution truth while preserving the explicit Wave 2 no-execution posture.
- `shell/src/run_workspace_allocation.rs` now materializes the real fresh isolated workspace allocation path beneath the local run-workspace root, including per-run directory creation and no-active-reuse enforcement via unique workspace paths.
- `shell/src/run_seed_ingestion.rs` now materializes execution-seed staging from the current approved Preflight lineage into the allocated run workspace by writing the current seed bytes and a run-start lineage manifest without embedding provider secrets, activation tokens, transcript replay payloads, or prior-run memory.
- `shell/src/run_session_start.rs` now materializes the real shell-owned fresh run/session creation and start-authorization path, including fresh run and session identifiers, blocked versus technical-failure versus execution-completed outcomes, real engine bootstrap plus moderated execution invocation, finished run-session snapshots, and rollback of partially allocated workspaces when staging or execution fails.
- Shell-owned activation contract surfaces now exist for activation request and response shape, approval versus denial versus technical-failure distinction, and bounded denial or failure classifications.
- Shell-owned activation storage-boundary behavior now exists for secure-store token handling, bounded local support-record shape, a JSON-backed app-controlled support-record store, and forbidden ordinary persistence paths.
- Shell-owned activation secure-store commit and retrieval behavior now exists through an OS-native secure-store abstraction that keeps raw token material out of ordinary files and frontend access.
- Shell-owned activation local-validation behavior now exists for current-device fingerprint, lineage, device-binding, expiration, and revoked-state checks against the committed secure token and bounded support record.
- Shell-owned activation-readiness truth now exists separately from runtime readiness and provider readiness and currently resolves to not-ready in this local environment because no approved activation is committed.
- Shell-owned activation command-boundary and activation-response-classification surfaces are now explicit in the local interface and trust-boundary scaffolds.
- A bounded Python commercial backend package now exists under `commercial_backend/` with deterministic SQLite migrations and durable license, payment-event, activation, activation-audit, and token-lineage records.
- The commercial backend now truthfully translates bounded payment-event inputs into license records, enforces the hard two-device rule with same-device slot reuse, distinguishes approval versus denial versus technical failure, and persists token fingerprints rather than raw tokens.
- The commercial backend now also exposes a bounded repo-local CLI interface for payment-event ingestion and activation-request evaluation without choosing a hosted deployment or public API surface.
- `shell/src/activation_contract.rs` now reflects the backend approval payload requirements for plan tier, device binding, issuance timestamp, signing lineage, and optional expiration claims.
- `shell/src/main.rs` now exposes both the bounded local activation-commit command and the real shell-owned activation submission command through the Tauri shell boundary.
- `shell/src/main.rs` now also exposes provider configuration state and provider configuration submission commands, alongside the existing provider contract, boundary, and readiness note surfaces, through the Tauri shell boundary.
- `shell/src/main.rs` now also exposes Preflight contract and readiness note surfaces, a shell-owned Preflight readiness-state command, the real bounded `submit_preflight` command, and the bounded `accept_preflight_downgrade` command through the Tauri shell boundary.
- `shell/src/main.rs` now also exposes Phase 6 Wave 2 run/session contract, workspace-boundary, start-lineage, and run-start-readiness note surfaces plus a shell-owned `run_start_readiness_state` command through the Tauri shell boundary.
- `shell/src/main.rs` now also exposes a real shell-owned `start_run_session` command plus `run_session_state` through the Tauri shell boundary for the bounded Wave 3 fresh-run path.
- `shell/src/main.rs` now also exposes `runtime_persona_contract_state` and `run_bounded_memory_state` so the frontend can render shell-owned Wave 4 foundation truth without gaining authority over persona generation or bounded memory.
- `shell/src/main.rs` now also exposes `run_moderated_rounds_note`, `run_moderated_rounds_state`, `run_token_duration_tracking_note`, and `run_token_duration_tracking_state` through the Tauri shell boundary so the frontend can render shell-owned Wave 5 execution and tracking truth without gaining execution authority.
- `shell/src/main.rs` now also exposes `run_lifecycle_state`, `run_status_state`, and `run_event_contract_state` so the frontend can consume the canonical Phase 7 lifecycle vocabulary, shell-owned status retrieval contract truth, and ordered event-envelope foundation without gaining authority over lifecycle meaning, event ordering, or cancelability.
- `shell/src/activation_submission.rs` now materializes the real shell-owned round trip between frontend activation input, current-device binding derivation, the repo-local commercial backend decision path, and the existing secure local activation commit behavior.
- `shell/src/startup_checks.rs`, `shell/src/app_lifecycle.rs`, `shell/src/app_session_trust_boundary.rs`, `shell/src/local_interface_contract.rs`, and `shell/src/engine_supervision.rs` now integrate the Phase 6 Wave 2 run/session, workspace-isolation, and run-start foundation surfaces while preserving shell-only run-start authority and separation from runtime, activation, provider-readiness, Preflight, and active-execution truth.
- `shell/src/startup_checks.rs`, `shell/src/app_lifecycle.rs`, `shell/src/app_session_trust_boundary.rs`, `shell/src/local_interface_contract.rs`, and `shell/src/engine_supervision.rs` now also reflect the Wave 5 one-shot moderated execution path, speaker attribution, bounded continuity mutation, run-local token/duration tracking, and the completed T05 in-flight monitoring basis while still not claiming interruption control or asynchronous supervision.
- `shell/src/run_lifecycle_state.rs`, `shell/src/run_status_contract.rs`, and `shell/src/run_event_contract.rs` now freeze the canonical Phase 7 lifecycle vocabulary, current status retrieval shape, cancellation request/response basis, deterministic event-envelope fields, retrieval cursor semantics, and no-frontend-authority boundary without materializing event persistence, live status streaming, or cancellation execution.
- `shell/src/run_session_contract.rs`, `shell/src/app_lifecycle.rs`, `shell/src/app_session_trust_boundary.rs`, `shell/src/startup_checks.rs`, `shell/src/local_interface_contract.rs`, and `shell/src/engine_supervision.rs` now integrate the completed Phase 7 Wave 2 contract foundation while preserving shell-owned lifecycle truth, shell-owned cancelability truth, and non-materialized event history or controlled cancellation behavior.
- `shell/src/run_session_start.rs`, `shell/src/run_lifecycle_state.rs`, and `shell/src/run_event_history.rs` now also materialize the bounded T05 shell prerequisite by publishing in-flight run/session checkpoint truth, projecting `Ingesting` and `GeneratingPersonas` from those checkpoints, and distinguishing active pending-history conditions from true post-run gap conditions.
- `shell/src/runtime_persona_contract.rs`, `shell/src/run_bounded_memory.rs`, `shell/src/run_moderated_rounds.rs`, and `shell/src/run_token_duration_tracking.rs` now define the shell-visible Wave 4 and Wave 5 contract truth for engine-owned runtime persona generation, bounded memory, moderated rounds, and run-local tracking.
- `engine/src/miro_fish_engine/session_execution.py`, `engine/src/miro_fish_engine/moderated_execution.py`, and `engine/src/miro_fish_engine/token_duration_tracking.py` now materialize the bounded Wave 5 engine execution path for Moderator-controlled rounds, 3-to-5-speaker selection, speaker-attributed turns, bounded continuity mutation, and run-local token/duration tracking.
- `engine/src/miro_fish_engine/bounded_memory.py` and `engine/src/miro_fish_engine/transcript_archive.py` now also materialize bounded continuity mutation and append-only speaker-turn capture plus archive-index updates during real execution.
- The frontend now exposes a bounded provider configuration workflow alongside the existing activation workflow, invokes the real shell-owned provider configuration command, renders shell-owned provider-state truth, and still does not receive raw provider secrets after submission.
- The frontend now also exposes a bounded single-document Preflight submission workflow, invokes the real shell-owned `submit_preflight` and `accept_preflight_downgrade` commands, renders approved versus approved-with-downgrade versus rejected versus technical-failure outcomes plus current invalidation truth, and shows shell-owned requested-plan versus effective-plan state without implying run or session execution.
- `app/src/App.tsx` now also surfaces the bounded Phase 6 Wave 2 run-start contract foundation in the frontend copy without adding any new frontend authority over run/session identity, workspace allocation, or active execution truth.
- `app/src/App.tsx` now also invokes the shell-owned Wave 3 `run_start_readiness_state`, `run_session_state`, and `start_run_session` commands to render truthful run-start readiness plus shell-owned run/session identity from the bounded one-shot execution path without implying push streaming, degraded-state handling, or controlled cancellation behavior.
- `app/src/App.tsx` now also invokes the shell-owned Wave 4 `runtime_persona_contract_state` and `run_bounded_memory_state` commands plus the Wave 5 `run_moderated_rounds_state` and `run_token_duration_tracking_state` commands and renders the engine-foundation and moderated-execution sections plus the expanded run-session snapshot without gaining authority over persona generation, Moderator decisions, speaker selection, or usage accounting.
- `app/src/App.tsx` now also invokes the shell-owned `run_lifecycle_state`, `run_status_state`, `run_event_contract_state`, and `run_event_history` commands and renders canonical lifecycle, status-contract, ordered event-contract, and distinct live-monitoring truth without inferring hidden lifecycle state, missing history, or cancellation semantics on its own.
- `app/src/App.test.tsx` and `app/src/App.wave4.test.tsx` now materialize assertion-backed frontend regression coverage for approval, rejection, technical failure, downgrade disclosure, downgrade acceptance, and stale invalidation under the repo-standard jsdom Vitest environment.
- `app/src/App.test.tsx` now also includes focused assertion coverage for shell-owned fresh run/session creation and finished Wave 5 run rendering under the bounded one-shot UI path.
- `app/src/App.test.tsx`, `app/src/App.wave4.test.tsx`, and `app/src/App.wave5.test.tsx` now also cover the Wave 4 foundation and Wave 5 moderated-round or tracking truth surfaces plus the expanded run-session snapshot fields.
- `app/src/App.wave7.test.tsx` plus the completed T05 shell tests now jointly cover the Phase 7 monitoring surface and the shell-owned in-flight snapshot publication prerequisite.
- `app/src/App.wave7.test.tsx` now adds focused coverage for the Phase 7 live-monitoring surface, chronology feed rendering, canonical lifecycle truth, status-contract truth, and ordered event-contract truth.
- `app/package.json` now runs `vitest run` without `--passWithNoTests`, and `app/vite.config.ts` now configures a jsdom Vitest environment for frontend DOM assertions.
- The Phase 4 critique pass found and fixed two material contradictions inside the implemented provider surface: hidden stale-provider secret persistence during provider switches and an unreachable `unsupported_provider` command-boundary classification.
- The canonical `ConfigurationRequired` lifecycle state and its upstream transition rules already exist and remain the bounded Phase 4 readiness target.
- The Phase 5 critique pass reran cleanly without identifying a material contradiction or in-scope defect that would block closure.
- `execution/current_phase.md`, `execution/handoffs/latest_handoff.md`, and `execution/evidence/latest_evidence.md` are retained only as legacy compatibility/archive surfaces.
- No product-feature implementation should be inferred from repo scaffolding alone.

## Current Known Backbone Choices
- Desktop shell technology: Rust/Tauri
- Frontend framework baseline: React with TypeScript
- Engine runtime/language: Python
- Repo test runner baseline: `cargo test`, `vitest`, and `pytest`

## Current Repository State
- `app/`: EXISTS — bounded React + TypeScript activation, provider-configuration, Preflight, Phase 6 run-start, Wave 4 engine-foundation, Wave 5 moderated-execution truth workflows, Phase 7 lifecycle/status/event plus live-monitoring rendering, closed Phase 8 report-access rendering, and completed Phase 9 cleanup/janitor contract plus Wave 3 cleanup-truth rendering on the Reports and run-detail surfaces that invoke shell-owned commands, render activation or provider or requested-versus-effective-plan or invalidation or runtime-persona or bounded-memory or moderated-round or tracking or lifecycle/status/event-contract or chronology or report-availability or local-access or cleanup/janitor truth, do not retrieve raw secure token, provider-secret, seed-content, or filesystem authority after submission, and include assertion-backed Vitest coverage across `app/src/App.test.tsx`, `app/src/App.wave4.test.tsx`, `app/src/App.wave5.test.tsx`, `app/src/App.wave7.test.tsx`, and `app/src/App.wave8.test.tsx`
- `shell/`: EXISTS — Rust/Tauri shell with startup, runtime, activation, provider, lifecycle, run-start, runtime persona, bounded-memory, moderated-round, tracking, canonical lifecycle, status-contract, event-contract, finalization, report-output, report-storage, report-access, run-cleanup-contract, cleanup-scope-mapping, startup-janitor-contract boundary surfaces, and real `run_cleanup_execution.rs` behavior plus fresh-run creation, workspace allocation, execution-seed staging, engine bootstrap invocation, shell-owned in-flight run/session snapshot publication, background-worker plus supervised-child execution control, bounded paused/retry handling, implemented cancellation and controlled-shutdown request paths, merged interruption chronology, truthful final run-session persistence, real terminal cleanup outcome classification, preserved-report survival, and durable report artifact plus local open/export behavior
- `commercial_backend/`: EXISTS — bounded Python commercial backend package with deterministic SQLite migrations, durable commercial ledger records, activation decision logic, a repo-local CLI bridge, and backend tests
- `engine/`: EXISTS — bounded Python engine package with deterministic runtime persona generation, Moderator basis construction, bounded memory initialization and mutation, transcript-archive speaker-turn capture, one-shot moderated execution, run-local token/duration tracking, report-data assembly, governed PDF generation, workspace bootstrap CLI, shell-authored execution-control polling at safe round/turn boundaries, structured cancellation or shutdown markers, and engine tests
- `tests/`: EXISTS — empty boundary only
- `docs/`: EXISTS — documentation/archive content only
- `execution/`: ESTABLISHED — lean live execution dashboard/log model present, packet registry present, Phase 4, Phase 5, Phase 6, Phase 7, Phase 8, Phase 9, Phase 10, and Phase 11 master execution blueprints present, and legacy mirrored state surfaces downgraded to compatibility/archive status

## Known Safe Assumptions
- The execution operating system files are authoritative only for repo execution control.
- They do not replace Documents A/B/C.
- The boundary folders do not settle framework, language, runtime, or packaging choices.
- The frozen backbone choices authorize Phase 2 scaffolding only; they do not authorize feature behavior.
- The final local support storage-engine choice may change implementation mechanism, but it must preserve the frozen Phase 3 activation-artifact split.
- Phase 4 entry no longer depends on choosing the final storage engine for any bounded non-secret provider-configuration support state, but any later choice must preserve the raw-secret secure-store-only rule.
- Wave 3 has now narrowed the repo-local minimum commercial backend implementation surface to the bounded Python-plus-SQLite package under `commercial_backend/`.
- Wave 4 has now narrowed the repo-local local-support engine to a bounded JSON support-record file in the app-controlled support domain while keeping the raw token in OS-native secure storage.
- The Phase 4 entry packet has now narrowed provider-secret handling to a shell-owned secure-store path plus an optional bounded non-secret support-state path in the app-controlled support domain.
- The Phase 4 Wave 2 packet has now narrowed the repo-local provider implementation basis to explicit contract, boundary, and readiness surfaces while keeping real provider setup behavior deferred.
- The Phase 4 Wave 3 packet has now materialized the real repo-local provider setup path without expanding into Preflight, provider/model reachability, or live inference behavior.
- The Phase 4 Wave 4 exit-gate packet completed after validating that the implemented provider path satisfies the governed acceptance targets without overclaiming Preflight or run readiness.
- The Phase 5 entry packet narrowed the repo-local Preflight basis to a shell-mediated one-seed intake path, explicit Preflight outcome classes, explicit requested-versus-effective plan semantics, explicit stale invalidation, and explicit run-start gating.
- The Phase 5 Wave 4 packet has now materialized bounded local Preflight feasibility and effective-plan behavior without expanding into live provider inference, live provider/model reachability probing, or run/session execution.
- Real run/session creation, fresh isolated workspace allocation, execution-seed staging, runtime persona generation, Moderator basis construction, bounded memory foundation, moderated round execution, bounded memory mutation during rounds, run-local token or duration tracking, shell-owned in-flight run/session identity plus pending-history truth, paused/degraded exposure, bounded retry handling, controlled cancellation, graceful-then-forceful active-run shutdown coordination, Phase 8 final-report generation, shell-owned local report availability/access behavior, and Phase 9 Wave 3 terminal cleanup hardening now exist in product code.
- `execution/phase_09_master_execution_plan.md` now narrows the later implementation basis to cleanup triggering, cleanup-result truth, shutdown-to-cleanup integration, startup janitor behavior, report-preservation exclusions, and no-resume enforcement without authorizing updater/release hardening or crash-resume.
- `execution/phase_10_master_execution_plan.md` now narrows the later implementation basis to native app-updater discovery and truthful updater states, runtime delivery authorization and artifact verification, compatibility and apply or use gating, and active-run non-mutation without authorizing release-readiness signoff or simulation, report, or cleanup redesign.
- `execution/phase_11_master_execution_plan.md` now narrows the later implementation basis to repo-local matrix instantiation, acceptance/regression execution, priority red-team review, blocker and residual-issue accounting, and the governed release verdict without authorizing new MVP features or an independent technical release authority.
- The Phase 11 entry packet interprets the usable-form matrix basis required by Document B Section `6.13.4` as the already-frozen combination of Document A acceptance and risk IDs, Document B validation and release-gate rules, and Document C Annex Family E matrix and aggregation structures; `P11-WS11-T02` has now materialized the corresponding repo-local working artifacts for later evidence packets to populate.
- `P11-WS11-T02` materialized the repo-local Phase 11 validation artifact pack under `execution/phase_11_validation/`, and `P11-WS11-T03`, `P11-WS11-T04`, `P11-WS11-T05`, and `P11-WS11-T06` have now populated and finalized those files with acceptance, regression, red-team, blocker, and verdict evidence rather than redesigning their structure.
- `P11-WS11-T03` populated the repo-local acceptance mapping and QA/regression matrix with real W03 evidence, `P11-WS11-T04` then populated the red-team matrix plus blocker/release aggregation surfaces, `P11-WS11-T05` recorded the historical `BL-001` blocker and `release-blocked` verdict under the earlier Windows-and-macOS contract, and `P11-WS11-T06` refreshed the final release record after the governed Windows-only scope revision, resolved `BL-001`, and closed Phase 11 as `release-ready`.
- The completed Phase 7 master plan now preserves the frozen implementation basis for canonical run lifecycle exposure, deterministic ordered event retrieval, live monitoring UI truth, the shell-owned in-flight snapshot prerequisite, and degradation/cancellation integration beneath the newly opened Phase 8 work.
- The completed Phase 7 Wave 2 packet now establishes the canonical state family, status retrieval shape, cancellation request/response basis, and event-envelope contract foundation as the truthful baseline for later Phase 7 packets while keeping real event history and interruption behavior deferred.
- The completed Phase 7 Wave 4 packet now serves as the implemented frontend live-monitoring UI basis over the completed shell-owned lifecycle, status, and ordered event-history retrieval surfaces.
- The completed Phase 7 Wave 5 packet now serves as the shell-owned in-flight run/session snapshot publication and monitoring-availability basis beneath the newly opened Phase 8 report/output work.
- The completed Phase 7 Wave 6 packet now serves as the implemented basis for paused/degraded truth, bounded retry behavior, controlled cancellation execution, and active-run shutdown integration beneath the newly opened Phase 8 report/output work.
- The completed Phase 7 closure packets now serve as the most recent runtime validation evidence and the truthful handoff into Phase 8 finalization/report-generation planning.
- `execution/phase_08_master_execution_plan.md` now narrows the later implementation basis to finalization entry handling, report data assembly, PDF generation, report availability/access truth, and report-versus-cleanup distinction without authorizing cleanup hardening or hosted report scope.
- The supported seed-document constants and the MVP runtime/update split are now frozen upstream and must be consumed as-is by later implementation packets.
- Hosted release infrastructure, signing automation, and distribution-operations details remain narrower deferred questions even though the base runtime/update split is now locked.
- Missing implementation details remain unknown until later packets explicitly govern them.

## Known Execution Gate Status
- `P11-WS11-T06` completed a governed scope-realignment refresh across Document A, the populated Phase 11 validation artifacts, and the live execution surfaces, then resolved the historical platform blocker and refreshed the current final verdict without product-code changes or new reruns.
- `P11-WS11-T05` completed a final evidence aggregation across the remaining platform, UI, diagnostics, and summary rows, updated the blocker and release artifacts, and then completed a structural consistency review plus contradiction check across the touched execution surfaces.
- `P11-WS11-T04` completed a scenario-by-scenario review across RT-001 through RT-012, updated the Phase 11 red-team, blocker, acceptance, QA, and release artifacts, and then completed a structural consistency review plus contradiction check across the touched execution surfaces.
- `P11-WS11-T03` reran the shell, frontend, engine, and commercial-backend validation families cleanly and then completed a structural consistency review plus contradiction check across the populated Phase 11 evidence artifacts and live execution surfaces.
- `P11-WS11-T02` was execution-doc-only and used a structural consistency review and contradiction check across the new Phase 11 validation artifacts plus the touched execution-state files rather than shell, frontend, engine, or commercial-backend code gates.
- `P11-WS11-T01` was execution-doc-only and used a structural consistency review and contradiction check across the changed planning and execution-state files rather than shell, frontend, engine, or commercial-backend code gates.
- `P10-WS11-T01` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall commercial_backend/src commercial_backend/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check commercial_backend`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest commercial_backend/tests` cleanly while mapping `AC-101`, `AC-102`, and materially touched `AC-105` through `AC-108` against the live Phase 10 implementation; no in-scope remediation was required before honest Phase 10 closure.
- `P10-WS10-T04` has so far rerun `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` cleanly for its first shell-only slice and its follow-on critique-remediation pass after in-scope remediation of a runtime-hydration ownership issue, an apply-gate exposure gap that was resolved by exposing a real shell command, a redundant-closure Clippy finding, a runtime-presence determined-missing-state defect, rollback risk on metadata-persistence failure, trust-manifest overacceptance, and hydration-required truth that previously overrelied on metadata alone.
- `P05-WS11-T01` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly while performing the Phase 5 acceptance mapping and critique pass; no in-scope code remediation was required before closure.
- `P06-WS11-T01` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall engine/src engine/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check engine`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest engine/tests` cleanly while performing the Phase 6 acceptance mapping and critique pass; no in-scope code remediation was required before closure.
- `P07-WS07-T01` was execution-doc-only and used a structural consistency review and contradiction check across the changed planning and execution-state files rather than shell, frontend, or engine code gates.
- `P07-WS07-T02` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly while materializing the canonical lifecycle, status-contract, and ordered event-contract foundation surfaces; no in-scope code remediation was required before closure.
- `P07-WS07-T04` reran `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly while implementing the bounded frontend monitoring UI, chronology feed, and event-history polling surface; the prior shell-owned closeout blocker was later resolved by completed packet `P07-WS07-T05`.
- `P07-WS07-T05` reran `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` cleanly while materializing the shell-owned in-flight snapshot publication path, pending-history distinction, and bounded Phase 7 truth-surface refinements.
- `P07-WS07-T06` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall engine/src engine/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check engine`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest engine/tests` cleanly while materializing paused/degraded truth, bounded retry handling, controlled cancellation execution, and active-run shutdown coordination.
- `P07-WS11-T01` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall engine/src engine/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check engine`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest engine/tests` cleanly while denying Phase 7 closure because runtime budget-hit/duration-cap control and truthful capped/finalizing lifecycle handling are not yet evidenced.
- `P07-WS11-T02` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall engine/src engine/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check engine`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest engine/tests` cleanly while materializing runtime cap enforcement, truthful constrained completion, shell-owned capped/finalizing projection, and aligned frontend monitoring truth.
- `P07-WS11-T03` reran `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` cleanly while performing the final recovered Phase 7 revalidation pass and fixing the last in-scope shell Clippy issue before honest closure.
- `P08-WS08-T01` was execution-doc-only and used a structural consistency review and contradiction check across the changed planning and execution-state files rather than shell, frontend, or engine code gates.
- `P08-WS08-T02` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly while materializing the bounded Phase 8 finalization/report contract foundation and frontend truth surface.
- `P08-WS08-T03` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall engine/src engine/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check engine`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest engine/tests` cleanly while materializing Wave 3 report-data assembly, report-generation failure classification, and the bounded frontend truth update.
- `P08-WS08-T04` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall engine/src engine/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check engine`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest engine/tests` cleanly while materializing real PDF generation, durable report artifact output, shell-owned report availability and local access behavior, and bounded frontend report-access rendering.
- `P08-WS11-T01` reran the same full Phase 8 shell, frontend, and engine gate set cleanly during the exit-gate pass after one in-scope frontend lint-baseline remediation and also executed a real artifact-layer smoke proving successful and constrained-completion report generation.
- `P09-WS09-T01` was execution-doc-only and used a structural consistency review and contradiction check across the changed planning and execution-state files rather than shell, frontend, or engine code gates.
- `P09-WS09-T02` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly while materializing cleanup/janitor contract foundation, cleanup-scope mapping, preserved-report exclusions, and bounded Reports-screen rendering.
- `P09-WS09-T03` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` cleanly while materializing real terminal cleanup triggering, cleanup-result classification, preserved-report survival, cleanup-aware lifecycle/status/event truth, and bounded frontend rendering.
- The `P09-WS09-T03` critique pass and touched execution-surface contradiction check completed cleanly without material findings before closeout.
- `P06-WS06-T05` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall engine/src engine/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check engine`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest engine/tests` cleanly before closeout.
- `P06-WS06-T04` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall engine/src engine/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check engine`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest engine/tests` cleanly before closeout.
- `P05-WS05-T04` ran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly after materializing bounded provider/model planning-context evaluation, effective-plan derivation, downgrade acceptance, stale invalidation, and the second frontend test file.
- `P05-WS05-T03` ran `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` cleanly after adding real shell-owned Preflight document validation, submission-state handling, and shell command exposure.
- `P05-WS05-T03` also ran `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly after updating the frontend to submit one seed document and render the shell-owned Preflight boundary state. The frontend test command completed under `--passWithNoTests` because no frontend test files existed yet at that closeout.
- `P05-WS05-T02` reran `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` cleanly after adding the Preflight contract, readiness, lifecycle, startup, trust-boundary, local-interface, and shell-command surfaces.
- `P05-WS05-T02` also reran `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly after updating the frontend to render the shell-owned Preflight readiness truth surface. The frontend test command completed under `--passWithNoTests` because no frontend test files existed yet at that closeout.
- `P05-WS05-T01` was execution-doc-only and used a structural consistency review and contradiction check rather than shell, frontend, or backend code gates.
- `P04-WS11-T01` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly after the critique remediation that restored truthful provider-switch cleanup and shell-boundary unsupported-provider classification.
- `P04-WS04-T03` ran `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` cleanly after adding provider secure-store, provider support-state persistence, provider submission and retrieval mediation, and truthful provider-readiness wiring.
- `P04-WS04-T03` also ran `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly after updating the frontend to exercise the real provider configuration path and render shell-owned provider state.
- `P04-WS04-T02` ran `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` cleanly after materializing the provider contract, boundary, readiness, lifecycle, startup, trust-boundary, and shell-command note surfaces.
- `P04-WS04-T02` also ran `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly after updating the frontend truth surface.
- `P03-WS11-T03` reran `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m compileall commercial_backend/src commercial_backend/tests`, `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m ruff check commercial_backend`, and `d:/PROJECTS/miro-fish-desktop-saas/.venv/Scripts/python.exe -m pytest commercial_backend` cleanly before Phase 3 closure.
- Wave 5 reran the full Phase 3 gate set cleanly: `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, Python compile validation for `commercial_backend/`, `ruff check commercial_backend`, `pytest commercial_backend`, `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build`.
- Wave 4 reran `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` cleanly after adding secure-store, support-store, local-validation, lifecycle, and interface wiring.
- Wave 4 reran `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` cleanly after the frontend placeholder truth update.
- The repo now has a backend-specific Wave 3 validation baseline using Python compile validation, `ruff`, and `pytest` against `commercial_backend/`.
- The repo now also has an engine-specific validation baseline using Python compile validation, `ruff`, and `pytest` against `engine/`.
- The canonical repo-level validation baseline is now the applicable subset of the shell gate family, the frontend gate family, the commercial-backend gate family, and the engine gate family already proven across completed packets.
- Wave 3 reran the backend gate set cleanly and reran `cargo check`, `cargo clippy -- -D warnings`, and `cargo test` cleanly after the shell activation-contract refinement.
- The shell gate baseline now exists locally with `cargo check`, `cargo clippy -- -D warnings`, and `cargo test`.
- The frontend enterprise gate baseline now exists locally with `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build`.
- `app/package.json` now resolves on `vite` 8.0.8, `vitest` 4.1.4, and `@vitejs/plugin-react` 6.0.1 after T03 remediation.
- `app/src/App.tsx` and `app/src/main.tsx` are diagnostics-clean in the current local environment.
- `npm audit` is currently clean in the frontend dependency tree after the T03 remediation work.
- The frontend gate baseline remains clean after the T03 dependency upgrades plus the T04/T05/T06/T01 placeholder truth updates.
- The shell gate baseline remains clean after T01 added the runtime-package-contract basis and after Wave 3 refined activation-contract semantics.
- The T01 packet reran the full shell and frontend gate set successfully before Phase 2 closure was recorded.
- P02-WS12-T04 was execution-doc-only and used a structural consistency review and contradiction check rather than code gates.
- The repo can now truthfully distinguish contract-defined runtime-ready from runtime-not-ready conditions; the current repo state resolves to runtime-not-ready because the approved runtime package is not present and runtime integrity is not accepted.
- Simulation features remain unavailable while runtime integrity is not accepted.
- The shell authority base is explicit across trust-boundary, interface-contract, supervision, controlled-shutdown, and runtime-package-contract surfaces, with active-run interruption control now materialized while broader runtime-package behavior remains separately scoped.

## Explicit Unknowns
- final hosted deployment surface for the commercial backend beyond the repo-local bounded Python package
- concrete payment-provider integration mechanism for real payment-event ingestion
- final hosted release infrastructure and signing automation for runtime/update artifacts
- final packaging and distribution automation details
- detailed frontend state/router/library choices
- detailed Python dependency management choice
- exact CI and coverage workflow details

## Explicitly Not Yet Implemented
- live provider/model reachability probing
- provider-dependent local execution behavior that would consume the mediated provider-secret retrieval path
- desktop shell behavior for active moderated execution beyond the bounded one-shot moderated run path and coarse shell truth surfaces
- app lifecycle execution-control behavior beyond the current bounded shell truth surfaces
- startup checks as fully productionized orchestration beyond the current bounded shell truth surfaces
- real external payment-provider ingress behavior
- runtime readiness as fully productionized behavior beyond the bounded shell-owned contract and verification basis
- populated Phase 11 acceptance mapping evidence, regression outcomes, or red-team evidence
- blocker or residual-known-issue entries derived from real Phase 11 findings
- final Phase 11 release-readiness aggregation results or a governed release verdict
- commercial-backend-backed runtime authorization or retrieval behavior beyond the current local allowlist-manifest input
- local interface authentication behavior
- automatic app-close interception behavior beyond the bounded shell-owned controlled-shutdown request surface
- hosted or push-streaming supervision behavior beyond the bounded local child-process control path
- frontend behavior beyond the bounded activation, provider-configuration, Preflight, Phase 6 run-start, Wave 5 truthful run-status rendering, and the bounded Phase 7 monitoring rendering surface
- engine behavior beyond the bounded Phase 6 one-shot moderated run path
- hosted release infrastructure, signing automation, and distribution-operations automation beyond the bounded product behavior already implemented locally
- product features, endpoints, UI behavior, or hidden service surfaces

## Update Rule
Every completed packet must update this file with:
- what now exists
- what remains stubbed
- what remains unknown
- what is explicitly not yet implemented